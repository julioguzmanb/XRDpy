
# macOS metadata
.DS_Store

# Manual build sources, local figures, and render artifacts are local by default.
# The public PDF is intentionally tracked and included in distribution metadata.
docs/manual/*
!docs/manual/
!docs/manual/XRDpy_manual.pdf
tmp/

# Local backup files
README.md.bak_*
